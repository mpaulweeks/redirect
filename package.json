{
  "name": "redirect",
  "version": "0.1.0",
  "repository": "git@github.com:mpaulweeks/redirect.git",
  "author": "mpaulweeks <mpaulweeks@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "api": "cd packages/api && yarn run start",
    "fe": "cd packages/fe && yarn run start",
    "setup": "yarn install && cd packages/api && yarn install && cd ../fe && yarn install",
    "deploy": "cd packages/api && yarn run deploy && cd ../fe && yarn run deploy"
  }
}
