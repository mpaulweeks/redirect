{
  "name": "redirect",
  "version": "0.1.0",
  "repository": "git@github.com:mpaulweeks/redirect.git",
  "author": "mpaulweeks <mpaulweeks@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "api": "cd pkg/api && yarn run start",
    "fe": "cd pkg/fe && yarn run start",
    "install-all": "yarn install && cd pkg/api && yarn install && cd ../fe && yarn install",
    "deploy": "cd pkg/api && yarn run deploy && cd ../fe && yarn run deploy"
  }
}
